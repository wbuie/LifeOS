{
  "name": "life-os",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync": "npm run dev --workspace=packages/sync",
    "import": "npm run import --workspace=packages/sync",
    "app": "npm run start --workspace=packages/app"
  }
}
